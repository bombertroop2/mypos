<td colspan="4">
  <div style="padding-top: 20px"></div>
  <%= form_for(@coa_type, remote: true, html: {class: "form-horizontal"}) do |f| %>
      <div class="form-group<%= control_group_error(@coa_type, :code) %>">
        <%= f.label :code, class: "col-sm-3 control-label" %>
        <div class="col-sm-9">
          <%= f.text_field :code, class: "form-control upcase inputs", autofocus: true%>
          <%= error_help_text(@coa_type, :code) %>
        </div>
      </div>
      <div class="form-group<%= control_group_error(@coa_type, :name) %>">
        <%= f.label :name, class: "col-sm-3 control-label" %>
        <div class="col-sm-9">
          <%= f.text_field :name, class: "form-control inputs" %>
          <%= error_help_text(@coa_type, :name) %>
        </div>
      </div>
      <div class="form-group<%= control_group_error(@coa_type, :description) %>">
        <%= f.label :description, class: "col-sm-3 control-label" %>
        <div class="col-sm-9">
          <%= f.text_area :description, class: "form-control inputs" %>
          <%= error_help_text(@coa_type, :description) %>
        </div>
      </div>

  <div class="form-group">
      <div class="col-sm-offset-3 col-sm-9">
        <%= f.submit "Save", class: "btn btn-primary" %>
        <button class="btn btn-link cancel">Cancel</button>
      </div>
    </div>
  <% end %>
</td>

<script>
  $(function () {
      $('#department_code').on("input", function () {
          $(this).val($(this).val().replace(/ /g, ""));
      });
      $("#department_company_id").attr("data-placeholder", "Please select").chosen();
  });
 </script>
