<td><%= object.code %></td>
<td id="product-brand-name-<%= object.id %>"><%= object.brand_name rescue nil %></td>
<td id="product-vendor-name-<%= object.id %>"><%= object.vendor_name rescue nil %></td>
<td id="product-model-name-<%= object.id %>"><%= object.models_name rescue nil %></td>
<td id="product-goods-type-name-<%= object.id %>"><%= object.goods_type_name rescue nil %></td>
<td class="actions" style="text-align: right">
    <%= link_to "<i class=\"glyphicon glyphicon-share-alt\"></i>".html_safe, product_path(object), data: {remote: true}, title: "Show" %>
    <%= link_to_if(can?(:manage, Product), content_tag(:i, "", :class => "glyphicon glyphicon-pencil"), edit_product_path(object), :remote => true, class: :edit, method: :get, title: "Edit" ) %>
      <%= link_to_if(can?(:manage, Product) && !object.purchase_order_relation.present? && !object.direct_purchase_product_relation.present?, content_tag(:i, "", :class => "glyphicon glyphicon-trash"), object, :remote => true, :class => "destroy", :method => :delete, :data => {:confirmation => 'Are you sure that you want to permanently delete the selected item?', placement: :left}, title: "Delete" ) %>
  </td>
