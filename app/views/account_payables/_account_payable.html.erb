<td><%= object.number %></td>
<td><%= object.payment_date.strftime("%d/%m/%Y") %></td>
<td id="vendor_name_<%= object.id %>"><%= object.name rescue "" %></td>
<td><%= number_to_currency(object.amount_paid,
      :separator => ",",
      :delimiter => ".",
      :unit => "Rp",
      :precision => 2) %></td>
<td class="actions" style="text-align: right">
    <% if user_signed_in? && is_admin? %>
      <%= link_to "<i class=\"glyphicon glyphicon-trash\"></i>".html_safe, object, data: {remote: true, confirmation: 'Are you sure?', placement: :left}, method: :delete, class: "destroy", title: "Delete" %>
      <% end %>
  </td>
