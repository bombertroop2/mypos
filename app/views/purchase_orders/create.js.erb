<%= smart_listing_item :purchase_orders, :create, @purchase_order, 
  @purchase_order.valid? ? "purchase_orders/purchase_order" : "purchase_orders/form" %>

<% if @purchase_order.valid? && @vendor_name.present? %>
  $("#vendor-name-<%= @purchase_order.id %>").html("<%= @vendor_name %>");
<% end %>