<td><%= "#{object.code} - #{object.name}" rescue "" %></td>
<td><%= number_to_currency(object.cost, :separator => ",", :delimiter => ".", :unit => "Rp", :precision => 2) rescue "" %></td>
<td><%= object.effective_date.strftime("%d/%m/%Y") rescue "" %></td>
<td class="actions" style="text-align: right">
    <%= link_to "<i class=\"glyphicon glyphicon-share-alt\"></i>".html_safe, cost_price_path(object), data: {remote: true}, title: "Show" rescue "" %>
    <% if user_signed_in? && is_admin? %>
      <%= link_to "<i class=\"glyphicon glyphicon-pencil\"></i>".html_safe, edit_cost_price_path(object), data: {remote: true}, class: :edit, title: "Edit" %>
        <%= link_to_if(!object.created_purchase_order.present? && object.product.cost_count > 1, content_tag(:i, "", :class => "glyphicon glyphicon-trash"), cost_price_path(object), :remote => true, :class => "destroy", :method => :delete, :data => {:confirmation => 'Are you sure?', placement: :left}, title: "Delete" ) %>
      <% end %>
  </td>
