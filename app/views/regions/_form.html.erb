<%= form_for(@region, html: {class: "form-horizontal"}) do |f| %>
    <!--
    <% if @region.errors.any? %>
                  <div id="error_explanation">
                    <h2><%= pluralize(@region.errors.count, "error") %> prohibited this region from being saved:</h2>

                    <ul>
      <% @region.errors.full_messages.each do |message| %>
                                  <li><%= message %></li>
      <% end %>
                    </ul>
                  </div>
    <% end %>
    -->
    <div class="form-group<%= control_group_error(@region, :code) %>">
        <%= f.label :code, class: "col-sm-2 control-label" %>
          <div class="col-sm-10">
              <%= f.text_field :code, class: "form-control upcase inputs", tabindex: "1" %>
                <%= error_help_text(@region, :code) %>
            </div>
        </div>
        <div class="form-group<%= control_group_error(@region, :name) %>">
            <%= f.label :name, class: "col-sm-2 control-label" %>
              <div class="col-sm-10">
                  <%= f.text_field :name, class: "form-control capitalize inputs", tabindex: "2" %>
                    <%= error_help_text(@region, :name) %>
                </div>
            </div>
            <div class="form-group">
                <%= f.label :description, class: "col-sm-2 control-label" %>
                  <div class="col-sm-10">
                      <%= f.text_area :description, rows: 6, tabindex: "3", class: "form-control" %>
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-sm-offset-2 col-sm-10">
                        <%= f.submit "Save", class: "btn btn-default active" %>
                      </div>
                  </div>
                <% end %>
