<%= form_for(@region) do |f| %>
  <% if @region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@region.errors.count, "error") %> prohibited this region from being saved:</h2>

      <ul>
        <% @region.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code, class: "upcase inputs", tabindex: "1" %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, class: "capitalize inputs", tabindex: "2" %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, rows: 6, tabindex: "3" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
