<%= form_for(@event, remote: true, html: {class: "form-horizontal"}) do |f| %>
    <%= hidden_field_tag "event_type", "cash discount" %>
    <%= f.hidden_field :event_type, value: "Discount(Rp)" %>
    <div class="form-group<%= control_group_error(@event, :code) %>">
        <%= f.label :code, class: "col-sm-2 control-label" %>
          <div class="col-sm-10">
              <%= f.text_field :code, class: "form-control upcase inputs" %>
                <%= error_help_text(@event, :code) %>
            </div>

        </div>
        <div class="form-group<%= control_group_error(@event, :name) %>">
            <%= f.label :name, class: "col-sm-2 control-label" %>
              <div class="col-sm-10">
                  <%= f.text_field :name, class: "form-control inputs" %>
                    <%= error_help_text(@event, :name) %>
                </div>
            </div>

            <div class="form-group<%= control_group_error(@event, :start_date_time) %>">
                <%= f.label :start_date_time, "Start time", class: "col-sm-2 control-label" %>
                  <div class="col-sm-10">
                      <%= f.text_field :start_date_time, size: 10, readonly: true, class: "form-control", id: "event_start_date_time_discount_cash" %>
                        <%= error_help_text(@event, :start_date_time) %>
                    </div>
                </div>

                <div class="form-group<%= control_group_error(@event, :end_date_time) %>">
                    <%= f.label :end_date_time, "End time", class: "col-sm-2 control-label" %>
                      <div class="col-sm-10">
                          <%= f.text_field :end_date_time, size: 10, readonly: true, class: "form-control", id: "event_end_date_time_discount_cash" %>
                            <%= error_help_text(@event, :end_date_time) %>
                        </div>
                    </div>

                    <div class="form-group<%= control_group_error(@event, :cash_discount) %>">
                        <%= f.label :cash_discount, "Amount", class: "col-sm-2 control-label" %>
                          <div class="col-sm-10">
                              <%= f.text_field :cash_discount, "data-a-sep" => ".",
                                "data-a-dec" => ",", "data-a-sign" => "Rp", style: "text-align:right;", size: 16, class: "form-control" %>
                                <%= error_help_text(@event, :cash_discount) %>
                            </div>
                        </div>

                        <div class="form-group">
                            <label class="col-sm-2 control-label">Warehouses</label>
                            <div class="col-sm-10">
                                <table id="listing_event_warehouses_table_discount_cash" class="display">
                                    <thead>
                                        <tr>
                                            <th></th>
                                            <th id="code_th_cash_discount">Code</th>
                                            <th>Name</th>
                                        </tr>
                                    </thead>

                                    <tbody>
                                        <% @warehouses.each do |warehouse| %>
                                          <tr id="warehouse_<%= warehouse.id %>_discount_cash">
                                              <td></td>
                                              <td><%= warehouse.code %></td>
                                              <td><%= warehouse.name %></td>
                                          </tr>
                                        <% end %>
                                    </tbody>
                                </table>
                                <a class="btn btn-default active" id="generate_event_cash_warehouse_form" style="float: right;margin-top: 10px;">Generate form</a>
                            </div>
                        </div>

                        <div id="listing-warehouse-details-cash">
                            <% if action_name.eql?("create") || !@event.new_record? %>
                              <%= render partial: "warehouse_form", locals: {event_type: "cash discount"} %>
                            <% end %>
                        </div>


                        <div class="form-group">
                            <div class="col-sm-offset-2 col-sm-10">
                                <%= f.submit "Save", class: "btn btn-primary", id: "submit-btn-discount-cash" %>
                                  <button class="btn btn-link cancel">Cancel</button>
                              </div>
                          </div>
                        <% end %>

                        <script type="text/javascript">
                          $(function () {
                              $("#event_start_date_time_discount_cash").datetimepicker({
                                  format: "dd/mm/yyyy hh:ii",
                                  autoclose: true,
                                  minuteStep: 1
                              });
                              $("#event_end_date_time_discount_cash").datetimepicker({
                                  format: "dd/mm/yyyy hh:ii",
                                  autoclose: true,
                                  minuteStep: 1
                              });
                              var eventWarehousesDiscountCashDataTable = $('#listing_event_warehouses_table_discount_cash').DataTable({
                                  order: [1, 'asc'],
                                  dom: 'T<"clear">lfrtip',
                                  columns: [
                                      {data: null, defaultContent: '', orderable: false},
                                      {data: 'code'},
                                      {data: 'name'}
                                  ],
                                  tableTools: {
                                      sRowSelect: 'os',
                                      aButtons: ['select_all', 'select_none']
                                  },
                                  paging: false,
                                  info: false,
                                  scrollY: "250px",
                                  scrollCollapse: true
                              });

                              $("#generate_event_cash_warehouse_form").click(function (e) {
                                  if (eventWarehousesDiscountCashDataTable.rows('.selected').data().length == 0) {
                                      e.preventDefault();
                                      bootbox.alert({message: "You have not selected warehouse yet!", size: 'small'});
                                  } else {
                                      var warehouseIds = [];
                                      $.each(eventWarehousesDiscountCashDataTable.rows('.selected')[0], function (index, value) {
                                          warehouseIds.push(eventWarehousesDiscountCashDataTable.rows(value).nodes().to$().attr("id").split("_")[1]);
                                      });
                        <% if @event.new_record? %>
                                        $.get("/events/generate_warehouse_form", {
                                            warehouse_ids: warehouseIds.join(","),
                                            event_type: "cash discount"
                                        });
                        <% else %>
                                        $.get("/events/generate_warehouse_form", {
                                            warehouse_ids: warehouseIds.join(","),
                                            event_type: "cash discount",
                                            event_id: "<%= @event.id %>"
                                        });
                        <% end %>
                                  }

                              });

                              $("#submit-btn-discount-cash").click(function () {
                                  if ($("#listing-warehouse-details-cash").html().trim() == "") {
                                      bootbox.alert({message: "Please select product(s) from selected warehouse(s)", size: 'small'});
                                      return false;
                                  } else {
                                      var valid = true;
                                      if ($(".event_warehouse_id_fields_cash_discount").length > 0) {
                                          $(".event_warehouse_id_fields_cash_discount").each(function () {
                                              if ($("#event_product_fields_container_" + $(this).val() + "_cash_discount").length == 0) {
                                                  valid = false;
                                                  return false;
                                              } else if ($("#event_product_fields_container_" + $(this).val() + "_cash_discount").html().trim() == "") {
                                                  valid = false;
                                                  return false;
                                              }
                                          });
                                      } else
                                          valid = false;

                                      if (!valid) {
                                          bootbox.alert({message: "Please select product(s) from selected warehouse(s)", size: 'small'});
                                          return false;
                                      } else
                                          return true;
                                  }
                              });

                        <% @event.event_warehouses.each do |event_warehouse| %>
                          <% unless event_warehouse._destroy %>
                                  var e = jQuery.Event("click");
                                  e.ctrlKey = true;
                                  $("#warehouse_<%= event_warehouse.warehouse_id %>_discount_cash").find("td:first-child").trigger(e);
                          <% end %>
                        <% end %>

                              $('#event_cash_discount').autoNumeric('init');
                          });
                        </script>  