<% if @valid %>
  <%= smart_listing_item :vendors, :create, @vendor, "vendors/vendor" %>
<% else %>
  <% puts "errors => #{@vendor.errors.messages}" %>
  <%= smart_listing_item :vendors, :create, @vendor, "vendors/form" %>
<% end %>