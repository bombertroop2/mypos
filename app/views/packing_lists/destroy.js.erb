<% if @deleted %>
  <%= smart_listing_item :packing_lists, :destroy, @packing_list %>
<% else %>
  <% if @packing_list.errors[:base].present? %>
    bootbox.alert({message: "<%= @packing_list.errors[:base].join("<br/>") %>",size: 'small'});
  <% end %>
<% end %>
