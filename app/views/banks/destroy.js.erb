<% if @deleted %>
  <%= smart_listing_item :banks, :destroy, @bank %>
<% else %>
  <% if @bank.errors.present? && @bank.errors.messages[:base].present? %>
    bootbox.alert({message: "<%= @bank.errors.messages[:base].join("<br/>") %>",size: 'small'});
  <% end %>
<% end %>
