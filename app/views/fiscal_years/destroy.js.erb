<% if @deleted %>
  <%= smart_listing_item :fiscal_years, :destroy, @fiscal_year %>
<% else %>
  <% if @fiscal_year.errors[:base].present? %>
    bootbox.alert({message: "<%= @fiscal_year.errors[:base].join("<br/>") %>",size: 'small'});
  <% end %>
<% end %>
