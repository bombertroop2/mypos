<div class="page-header"><h1>Listing Sales Promotion Girls</h1></div>

<%= smart_listing_controls_for(:sales_promotion_girls, {class: "form-inline text-right"}) do %>

    <div class="form-group filter input-append">
        <%= text_field_tag :filter, '', class: "search form-control", 
            placeholder: "Search...", autocomplete: :off %>
      </div>

      <button class="btn btn-primary disabled" type="submit">
          <span class="glyphicon glyphicon-search"></span>
      </button>
    <% end %>
    <%= smart_listing_render(:sales_promotion_girls) %>
